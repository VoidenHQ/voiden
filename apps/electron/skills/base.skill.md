---
name: voiden
description: Create and edit Voiden .void files for API testing. Covers the .void file format and all enabled extension block types.
---

# Voiden .void File Format

## Overview

Voiden uses `.void` files for API testing. Each file uses a **hybrid format** combining:
- YAML frontmatter for metadata
- Markdown content for documentation
- Custom `void` code blocks for structured request/response data

## File Structure

```markdown
---
version: 0.20.1
generatedBy: Voiden app
note: This file is auto-generated by the Voiden app
generatedAt: 2025-01-15T10:30:00.000Z
---

# API Documentation Title

Description and test cases in Markdown...

```void
---
type: request
attrs:
  uid: 05119029-b827-45fe-a2cc-55a5f0ba6609
content:
  - type: method
    attrs:
      uid: 35d05fd6-1e12-4f6a-97d5-5c66d59bc61f
      method: GET
      visible: true
    content: GET
  - type: url
    attrs:
      uid: f84b83da-7fe7-415f-baa1-b045adfd7438
    content: https://api.example.com/users
---
```
```

## Frontmatter Fields

| Field | Value |
|-------|-------|
| `version` | Current Voiden version (e.g. `0.20.1`) |
| `generatedBy` | Always `"Voiden app"` |
| `note` | Always `"This file is auto-generated by the Voiden app"` |
| `generatedAt` | ISO 8601 timestamp |

## Void Block Structure

All blocks use triple-backtick `void` fences:

```yaml
---
type: [block-type]
attrs:
  uid: [UUID v4]
  [other-attrs...]
content:
  [child nodes or string value]
---
```

## Rules

- **UIDs**: Always UUID v4 — `crypto.randomUUID()` or `uuidgen`
- **Variables**: Use `{{VARIABLE_NAME}}` for environment variables
- **Indentation**: 2 spaces for YAML and JSON
- **Strings**: Quote values containing special characters, colons, or spaces
- **Timestamps**: ISO 8601 format (`2025-01-15T10:30:00.000Z`)
- **Block order inside `request`**: method → url → auth → headers-table → query-table → path-table → body → assertions → scripts

## Workflows

### Convert curl to .void

1. Parse HTTP method from `-X` flag (default GET)
2. Extract URL (from `--url`, bare argument)
3. Map `-H "Key: Value"` → `headers-table` rows
4. Map `?key=value` query params in URL → `query-table` rows
5. Map `-d '{...}'` → `json_body` (or `text_body` for non-JSON)
6. Map `-u user:pass` → `auth` block with `authType: basic`
7. Map `-H "Authorization: Bearer ..."` → `auth` block with `authType: bearer`
8. Generate fresh UUIDs for all `uid` attrs
9. Write ISO timestamp for `generatedAt`

### Generate from OpenAPI/Swagger spec

1. Identify all `paths` + HTTP methods combinations
2. Group related endpoints into one `.void` file by resource (e.g. `users.void`)
3. For each endpoint: create a `request` block with `method` + `url`
4. Add `path-table` for `{param}` segments
5. Add `query-table` for query parameters
6. Add `json_body` from the request body schema
7. Add `auth` matching the spec's `securitySchemes`
8. Write markdown documentation above each request block

## Multi-Request Files

A single `.void` file can contain multiple independent HTTP requests separated by `request-separator` nodes. Each section between separators has its own scope — endpoint, headers, body, auth, and assertions are fully isolated.

### request-separator — Section Divider

Insert with `/new-request`. Splits the document into independent sections. Each separator carries a label (the section name) and a `colorIndex` for visual distinction.

```yaml
---
type: request-separator
attrs:
  uid: "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  colorIndex: 0
  label: "Create User"
---
```

| Attr | Description |
|------|-------------|
| `uid` | UUID v4 — always generate a fresh one |
| `colorIndex` | Integer 0–N, pick a value unique within the file for distinct color coding |
| `label` | Section name shown in the UI (double-click to rename). Defaults to `"New Request"` |

**Slash command:** `/new-request`

### Rules

- Everything before the first separator is **section 0**.
- Each separator introduces a new section — blocks after it belong to that section.
- Singleton blocks (e.g. `request`, `json_body`) are enforced **per section**, not per file.
- **Run All** (`⌘⇧↵`) executes every section in document order, top to bottom.
- Clicking **Run** inside a section runs only that section.
- Variables set by a script in one section flow into subsequent sections (unless isolation is enabled by Stitch Runner).

> **UIDs in examples are illustrative only.** Always generate a fresh UUID v4 for every `uid`. Use a `label` that describes the section (e.g. `"Create User"`, `"Login"`). Pick a `colorIndex` that is unique within the file.

### Multi-Request File Example

```markdown
---
version: 0.20.1
generatedBy: Voiden app
note: This file is auto-generated by the Voiden app
generatedAt: 2025-01-15T10:30:00.000Z
---

# User CRUD

```void
---
type: request-separator
attrs:
  uid: "c3d4e5f6-a7b8-4c9d-e0f1-a2b3c4d5e6f7"
  colorIndex: 0
  label: "Create User"
---
```

```void
---
type: request
attrs:
  uid: "a1b2c3d4-e5f6-4789-ab01-cd23ef456789"
  content:
    - type: method
      attrs:
        uid: "b2c3d4e5-f6a7-4b8c-9d0e-f1a2b3c4d5e6"
        method: POST
        visible: true
      content: POST
    - type: url
      attrs:
        uid: "d4e5f6a7-b8c9-4d0e-f1a2-b3c4d5e6f7a8"
      content: "{{BASE_URL}}/users"
---
```

```void
---
type: json_body
attrs:
  uid: "e5f6a7b8-c9d0-4e1f-a2b3-c4d5e6f7a8b9"
  body: |
    { "name": "Alice", "email": "alice@example.com" }
---
```

```void
---
type: request-separator
attrs:
  uid: "f6a7b8c9-d0e1-4f2a-b3c4-d5e6f7a8b9c0"
  colorIndex: 1
  label: "List Users"
---
```

```void
---
type: request
attrs:
  uid: "a7b8c9d0-e1f2-4a3b-c4d5-e6f7a8b9c0d1"
  content:
    - type: method
      attrs:
        uid: "b8c9d0e1-f2a3-4b4c-d5e6-f7a8b9c0d1e2"
        method: GET
        visible: true
      content: GET
    - type: url
      attrs:
        uid: "c9d0e1f2-a3b4-4c5d-e6f7-a8b9c0d1e2f3"
      content: "{{BASE_URL}}/users"
---
```

## Importing Blocks from Other Files

Individual blocks can be imported from any other `.void` file using a `linkedBlock`. This lets you reuse headers, auth, query params, bodies, etc. across requests without duplicating them.

### linkedBlock — Import a Specific Block

```yaml
---
type: linkedBlock
attrs:
  uid: 1ac1db1b-26fa-486a-9d74-1f8db6b506be
  blockUid: 825cb614-99af-4d19-ac1e-7af0dcb2acf5
  originalFile: /blocks/headers.void
---
```

| Attr | Description |
|------|-------------|
| `uid` | Fresh UUID v4 generated for this `linkedBlock` instance |
| `blockUid` | The `uid` of the specific block being imported from the target file |
| `originalFile` | Absolute path to the target file from the **project root** (e.g. `/blocks/headers.void`) |

### How to Generate a linkedBlock

When asked to import a block from another file:

1. **Read the target `.void` file** to find the block the user wants (headers-table, auth, json_body, etc.)
2. **Copy its `uid`** — that becomes `blockUid`
3. **Generate a fresh UUID** for the `linkedBlock`'s own `uid`
4. **Set `originalFile`** to the path of that file from the project root

### Override Logic

**Local blocks always win over imported ones.** When the same key appears in both an imported block and a local block of the same type, the local value takes precedence. You can import a shared headers-table and add or override individual rows with a local headers-table in the same request.

### Examples

**Import headers from `/shared/base-headers.void`** (which contains a headers-table with uid `825cb614-...`):

```yaml
---
type: linkedBlock
attrs:
  uid: 1ac1db1b-26fa-486a-9d74-1f8db6b506be
  blockUid: 825cb614-99af-4d19-ac1e-7af0dcb2acf5
  originalFile: /shared/base-headers.void
---
```

**Import auth from `/auth/login.void`** (which contains an auth block with uid `f3a2b1c0-...`):

```yaml
---
type: linkedBlock
attrs:
  uid: 9d3e7f12-aa45-4b89-cd01-ef2345678901
  blockUid: f3a2b1c0-1234-5678-abcd-ef0123456789
  originalFile: /auth/login.void
---
```

**Import a JSON body from `/payloads/create-user.void`** (json_body uid `cc112233-...`):

```yaml
---
type: linkedBlock
attrs:
  uid: 7b6a5c4d-3e2f-1a0b-9c8d-7e6f5a4b3c2d
  blockUid: cc112233-4455-6677-8899-aabbccddeeff
  originalFile: /payloads/create-user.void
---
```

### `importedFrom` Attribute

Blocks that were pulled in via a `linkedBlock` carry `importedFrom` on their attrs (e.g. `importedFrom: "/shared/base-headers.void"`). This is set **automatically by Voiden** when resolving links — never set it manually when generating `.void` files.

### linkedFile — Import a File or Section

Use `linkedFile` to dynamically import a `.void` file (or a specific section of one) into the current document. The imported content is displayed read-only and collapsible, and runs as part of **Run All** in document order.

A `linkedFile` is always preceded by a `request-separator` that acts as its section boundary. The separator's `label` names the section; the `linkedFile` provides the content.

| Attr | Description |
|------|-------------|
| `uid` | Fresh UUID v4 for this `linkedFile` instance |
| `originalFile` | Path to the target file from the **project root** (e.g. `/auth/session.void`) |
| `sectionUid` | *(optional)* The `uid` of a `request-separator` in the target file. When set, only blocks from that section are imported. Omit (or `null`) to import the whole file. |

**Behaviour:**
- The imported content is shown read-only, collapsible, under a separator-styled header.
- When **Run All** is triggered, the linked content runs as part of the normal section sequence.
- Clicking the filename opens the linked file in its own tab.
- Changes to the target file are automatically reflected — no re-linking needed.
- If the target file's first block is a `request-separator`, it is dropped on expansion (the parent separator serves as the section boundary).

**Whole-file import** — use for single-section files (files containing no `request-separator` nodes):

```yaml
---
type: request-separator
attrs:
  uid: c18a4ff7-0f03-40d7-a82c-32e716968e34
  colorIndex: 0
  label: "session"
---
```

```yaml
---
type: linkedFile
attrs:
  uid: 79fcc235-7cca-4612-8053-dcc5d8691cc3
  originalFile: /auth/session.void
  sectionUid: null
---
```

**Section-specific import** — use for multi-section files; `sectionUid` is the `uid` of the target `request-separator`:

```yaml
---
type: request-separator
attrs:
  uid: c18a4ff7-0f03-40d7-a82c-32e716968e35
  colorIndex: 0
  label: "Login"
---
```

```yaml
---
type: linkedFile
attrs:
  uid: 14d9c721-f84b-4103-ad66-1d645218c469
  originalFile: /ike/web/auth/login_ADMIN_SSO.void
  sectionUid: 9830f1a2-cc45-4b67-de89-f01234567890
---
```

**How to generate a `linkedFile` in code:**
1. Read the target file to find the `request-separator` uid (for section imports) or confirm it has no separators (for whole-file imports).
2. Generate a fresh UUID for `uid`.
3. Set `originalFile` to the project-root-relative path.
4. Set `sectionUid` to the separator's `uid`, or `null` for whole-file.
5. Prepend a `request-separator` block with a descriptive `label`, with a colorIndex unique in the file.
6. Place the pair at the top level of the document (never inside a `request` block).

## File Naming

- kebab-case: `list-users.void`, `get-user.void`, `create-order.void`
- Group by resource in folders: `users/`, `orders/`, `auth/`
- One logical resource group per file; multiple request blocks per file is fine

## Environment Variables

Store secrets in `.env` alongside `.void` files:

```env
BASE_URL=https://api.example.com
API_TOKEN=your-token-here
USER_ID=123
```

Reference with `{{VARIABLE_NAME}}` in any block field:
- URL: `{{BASE_URL}}/users/{{USER_ID}}`
- Header value: `Bearer {{API_TOKEN}}`
- Body field: `"email": "{{USER_EMAIL}}"`
